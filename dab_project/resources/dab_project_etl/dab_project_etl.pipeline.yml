# The main pipeline for dab_project

resources:
  pipelines:
    dab_project_etl:
      name: dab_project_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "."

      libraries:
        - glob:
            include: transformations/**
