# This is a Databricks asset bundle definition for dab_project.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dab_project
  uuid: cf514caa-7dc8-4790-a9af-0a335edc7bd3

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-3480036697306527.7.azuredatabricks.net
    variables:
      catalog: dlt_catalog
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: https://adb-3480036697306527.7.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/jenny@xgao198902gmail.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/Users/jenny@xgao198902gmail.onmicrosoft.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: dlt_catalog
      schema: prod
    permissions:
      - user_name: jenny@xgao198902gmail.onmicrosoft.com
        level: CAN_MANAGE
